# Install this file as /etc/nginx/conf.d/peredacha-rate-limit.conf.
# Ubuntu's default nginx.conf includes conf.d/*.conf inside the http context.
limit_req_zone $binary_remote_addr zone=peredacha_general:10m rate=10r/s;
limit_req_zone $binary_remote_addr zone=peredacha_login:10m rate=5r/m;
